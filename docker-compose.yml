version: '3.8'

services:
  bot:
    build: .
    container_name: ebook-bot
    env_file:
      - .env
    restart: unless-stopped
    expose:
      - "3001"
    networks:
      - proxy

  webapp:
    build:
      context: src/webapp
      dockerfile: Dockerfile
    container_name: ebook-webapp
    restart: unless-stopped
    expose:
      - "80"
    networks:
      - proxy

networks:
  proxy:
    external: true
